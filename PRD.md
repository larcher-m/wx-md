# wx-md 产品需求文档（PRD）

> 版本：v1.0 草案
> 状态：需求规划
> 产品定位：通用的微信公众号 Markdown 渲染组件

---

## 1. 产品概述

### 1.1 背景与问题

微信公众号后台自带的编辑器排版能力较弱，作者习惯用 Markdown 写作，但把 Markdown 内容粘贴进公众号编辑器后，标题、列表、代码块等排版会丢失或变形，需要大量手动调整。

### 1.2 产品目标

构建一个**通用、可复用**的 Markdown 渲染组件（npm 包）：

1. 把 Markdown 源文件渲染为**符合公众号审美的漂亮页面**；
2. 支持**一键复制**，生成带内联样式的富文本，直接粘贴到公众号编辑器，排版不丢失；
3. **通用性**：不绑定任何前端框架，可在原生 JS、Vue、React 等任意环境使用。

### 1.3 目标用户

| 用户 | 场景 | 核心诉求 |
| --- | --- | --- |
| 公众号运营 / 作者 | 用 Markdown 写作后发布到公众号 | 粘贴即排版，无需手动调样式 |
| 前端开发者 | 在自己的产品中集成 | 安装即用、API 简单、可配置 |

### 1.4 术语定义

- **内联样式**：写在 HTML 标签 `style` 属性中的 CSS（不依赖外部样式表）。
- **富文本粘贴**：复制带格式的 HTML 内容，粘贴到编辑器后保留样式。
- **所见即所得（WYSIWYG）**：预览效果与最终粘贴到公众号的效果一致。

---

## 2. 产品范围

### 2.1 MVP 范围（v1.0）

| 模块 | 内容 |
| --- | --- |
| Markdown 渲染 | 全语法支持（见 FR-1） |
| 样式内联化 | 所有标签输出内联样式，适配公众号（见 FR-2） |
| 主题系统 | 内置 ≥3 套主题，可切换、可配置（见 FR-3） |
| 一键复制 | 复制为富文本 HTML，兼容公众号编辑器（见 FR-4） |
| 代码高亮 | 带内联样式的代码块高亮（见 FR-5） |
| 图片处理 | 本地图片自动转 base64 内嵌（见 FR-6） |
| 双端预览 | PC 预览 + 手机宽度模拟（见 FR-7） |

### 2.2 非 MVP（后续版本）

- 图床上传（腾讯云 / 阿里云 OSS）
- 在线编辑工具（打开即用的 Web 页面）
- CLI 命令行工具（.md → HTML 转换）
- 数学公式（KaTeX）、流程图（mermaid）
- 主题商店（社区共享主题）

---

## 3. 功能需求（FR）

### FR-1 Markdown 渲染

- 渲染引擎：`markdown-it` + 官方/社区插件。
- 支持的语法：
  - 标题（H1~H6）
  - 段落、加粗、斜体、删除线
  - 有序 / 无序 / 嵌套列表、任务列表
  - 引用、分割线、脚注
  - 行内代码、代码块（可带语言标识）
  - 表格（含对齐）
  - 链接、图片
  - 转义字符
- 不支持 / 降级处理的内容需有明确说明（如数学公式在 MVP 中降级为纯文本）。

### FR-2 样式内联化（核心）

- **背景**：微信公众号编辑器不加载外部 CSS/JS，只保留标签内联样式。
- 要求：
  - 通过自定义 `markdown-it` 的 renderer rules，为每个渲染标签生成内联 `style`。
  - 标题、段落、列表、引用、代码、表格、分割线等都必须有完整的内联样式。
  - 输出 HTML 中不依赖任何 `class` / `id` 或外部样式表。
  - 样式属性需避开公众号编辑器的过滤规则（如不依赖 `position` 定位、避免复杂嵌套标签）。

### FR-3 主题系统

- 内置 ≥3 套主题，例如：
  1. **极简白**：白底黑字，适配大多数文章；
  2. **微信绿**：公众号品牌色点缀；
  3. **暖色刊**：暖色调，适合人文类文章。
- 主题可配置项（默认值来自所选主题）：
  - 正文字号、行高、字间距
  - 标题字号与颜色
  - 主色 / 强调色（用于链接、引用边框、分割线）
  - 代码块背景与配色
- 切换主题后预览实时更新。

### FR-4 一键复制

- 一键生成富文本 HTML 并复制到剪贴板。
- 复制结果粘贴到微信公众号后台编辑器后，与预览效果一致（允许 ± 细微差异）。
- 兼容目标：公众号后台编辑器（MP 平台）、订阅号助手 App。
- 复制成功 / 失败需有明确反馈（如 Toast 提示）。

### FR-5 代码高亮

- 使用 `Prism.js` 或 `highlight.js` 生成**带内联样式的**高亮代码块。
- 支持常见语言：JavaScript、TypeScript、Python、Java、Go、Shell、CSS、HTML、JSON 等。
- 代码块包含：语言标识、背景色、行内高亮配色，且粘贴到公众号后配色不丢失。

### FR-6 图片处理

- 支持本地图片拖入 / 粘贴 / 选择上传。
- 本地图片自动转换为 base64 并内嵌到 HTML 中（粘贴后无需外链即可显示）。
- 图片大小校验：超出公众号限制（正文图片建议 ≤2MB）时给出提示，并可压缩后再插入。
- 支持远程图片 URL（直接使用原链接，不做处理）。

### FR-7 双端预览

- PC 端完整预览。
- 手机宽度模拟（默认 375px），查看移动端实际排版效果。
- 预览与复制结果基于同一套渲染管线，保证一致性。

---

## 4. 公众号平台限制与适配（关键约束）

| 限制 | 适配方案 |
| --- | --- |
| 不加载外部 CSS/JS | 全部样式内联到标签 |
| 单篇内容长度限制（约 100,000 字符） | 提供字数 / 长度统计提示 |
| 正文图片大小限制（建议 ≤2MB） | base64 图片体积检测 + 压缩提示 |
| 编辑器会过滤部分标签 / 属性 | 输出保守的 HTML 结构，避免依赖复杂嵌套与定位样式 |
| 表格 / 代码块兼容性 | 使用公众号兼容的简化写法，代码块用内联样式化 `<pre><code>` |

---

## 5. 组件 API 设计（npm 包）

目标形态：纯 JS（ESM + UMD），无框架依赖，可在任意项目中使用。

```js
import { render, copyToClipboard, THEMES } from "wx-md";

// 1. 渲染：Markdown 字符串 -> 带内联样式的 HTML 字符串
const html = render(markdownSource, {
  theme: "wechat-green",      // 主题名，见 THEMES
  fontSize: 16,               // 正文字号（px）
  lineHeight: 1.75,           // 行高
  imageBase64: true,          // 本地图片是否转 base64
});

// 2. 一键复制：将 HTML 作为富文本复制到剪贴板
await copyToClipboard(html);
```

- `render(md, options?)` → HTML 字符串：核心渲染入口。
- `copyToClipboard(html)` → Promise：富文本复制，含兼容降级方案。
- `THEMES`：内置主题列表。
- 配置项需提供合理默认值，开箱即用。

---

## 6. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 包体积 | gzip 后 ≤ 100KB（不含可选高亮语言包） |
| 兼容性 | Chrome / Edge / Safari / Firefox 最新两个大版本；不要求 IE |
| 性能 | 10 万字 Markdown 渲染耗时 < 1s |
| 易用性 | API 简单、开箱即用、有中文文档与示例 |
| 可维护性 | 代码含中文注释（沿用项目习惯） |

---

## 7. 验收标准（对应功能需求）

| 编号 | 验收项 | 通过条件 |
| --- | --- | --- |
| AC-1 | Markdown 渲染 | 覆盖 FR-1 全部语法，输出合法 HTML |
| AC-2 | 内联样式 | 输出 HTML 无外部 CSS 依赖，粘贴到公众号后样式完整 |
| AC-3 | 主题系统 | ≥3 套主题，切换后预览与复制结果均生效 |
| AC-4 | 一键复制 | 复制后粘贴进公众号后台，效果与预览一致 |
| AC-5 | 代码高亮 | 代码块高亮颜色在粘贴后保留 |
| AC-6 | 图片处理 | 本地图片 base64 内嵌后复制粘贴可正常显示；超限有提示 |
| AC-7 | 双端预览 | 手机模拟宽度下排版正常 |
| AC-8 | API | 按 §5 API 签名调用，开箱即用，无框架依赖 |

---

## 8. 后续规划

1. 需求评审：确认本 PRD 后，输出产品原型（Figma / 蓝湖）。
2. 技术设计：确定插件选型、内联样式生成方案、复制兼容方案。
3. 分阶段实现：核心渲染 → 主题 → 复制 → 图片 → 高亮 → 预览。
4. 测试与发布：覆盖公众号后台实机验证，发布到 npm。
